#include <stdio.h>

/**
 * main - Entry point, prints alphabets from a -z, exept q and e
 *
 * Return: returns zero
 **/

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)

		printf("%d", n);
	printf("\n");

	return (0);
}
