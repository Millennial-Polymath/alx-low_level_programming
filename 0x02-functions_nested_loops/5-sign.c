#include "holberton.h"

/**
 * print_sign - prints the sign of a number
 * @n: The value to be passed to the function
 *
 * Return: Returns 1 if n is greator than zero, 0 if zero and -1 if n is less
 * than zero
 */



int print_sign(int n);
{
	if (n > 0)
	{
		return (1);
	_putchar('+');
	}
	if else(n == 0)
	{
		return (0);
		_putchar(0);
	}
	else
	{
		return (-1);
	_putchar('-');
	}
}
